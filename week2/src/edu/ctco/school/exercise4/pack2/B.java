package edu.ctco.school.exercise4.pack2;

import edu.ctco.school.exercise4.pack1.*;
/**
 * Fix the code, so we are able to extend A.
 *
 * What are the options ?
 */
class B extends A {
    //to make class A public, so it can be then imported to the class B and extended.
    //second option would be to transfer class B to class A package, so that the packages would be the same for both classes.
}
